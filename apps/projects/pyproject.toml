[project]
name = "projects"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gradio>=5.8.0",
    "mymath",
]

[tool.uv.sources]
mymath = { workspace = true }
